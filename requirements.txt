# Core stack
langgraph>=0.2
langchain-core>=0.3
streamlit>=1.37
pandas>=2.2
pydantic>=2.7
python-dotenv>=1.0

# Live LLM reasoning (Claude Haiku 4.5 via AWS Bedrock -- the only LLM
# path this app uses; not required to run the app, only to move past
# the deterministic fallback). Set AWS credentials via the sidebar's
# "Connect Claude" panel, or in .env -- see README.
langchain-aws>=0.2
